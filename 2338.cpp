#include <bits/stdc++.h>
using namespace std;

class Solution {
    public:
        int idealArrays(int n, int maxValue) {
            const int N = 1e4 + 5;
            int a[N][N];

            for (int i = 0; i < N; i++) for (int j = 0; j < N; j++) a[i][j] = 0;

            for (int i = 1; i < N; i++) {
                for (int j = 1; j < N; j++) {
                    
                }
            }
        }
    };