#include <bits/stdc++.h>
using namespace std;

class Solution {
    public:
        long long countOfSubstrings(string word, int k) {
            int vowels = 0;
            int consonants = 0;
            for (int i = 0; i < k; i++) {
                if (word[i] == 'a' or word[i] == 'e' or )
            }
        }
    };