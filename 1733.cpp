#include <bits/stdc++.h>
using namespace std;

const int N = 505;
set<int> lang[N];

class Solution {
public:
    int minimumTeachings(int n, vector<vector<int>>& languages, vector<vector<int>>& friendships) {
        for (int i = 0; i < languages.size(); i++) lang[i].clear();

        for (int i = 0; i < lang) {
            for (auto j : i) {
                lang[i].inse
            }
        }
    }
};